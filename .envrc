# Odoo Skill Manager recommended environment (direnv + venv)
export ODOO_SKILL_MANAGER_ROOT="$(pwd)"

if [ ! -d .venv ]; then
  python3 -m venv .venv
fi

source .venv/bin/activate
export PATH="$ODOO_SKILL_MANAGER_ROOT/commands:$PATH"
