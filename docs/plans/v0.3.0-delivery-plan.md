# v0.3.0 Delivery Plan

Date: 2026-03-11
Status: done

## Goal
Make runtime project targeting deterministic by adding explicit `--project` support where cwd auto-detection was previously required.

## Scope
- Add `--project` to runtime commands:
  - automation: `components`, `enable-skill`, `disable-skill`
  - human-ops: `up`, `db`, `shell`, `test`, `lint`
- Keep existing behavior as fallback (cwd auto-detection) when `--project` is not provided.
- Update contract tests/docs.

## Completed
- [x] Added shared project resolver with explicit-path validation.
- [x] Added `PROJECT_OPTION` click option and wired commands.
- [x] Extended contract tests for `--project` usage.
- [x] Updated README and contract docs.
- [x] Added this delivery plan record.
